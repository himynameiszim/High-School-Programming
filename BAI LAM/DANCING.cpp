#include<bits/stdc++.h>

using namespace std;

const int M = 1e5 + 5;

int n, a[M], b[M];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    cin>>n;
    for(int i = 0; i < n; i++) cin >> a[i];
    for(int i = 0; i < n; i++) cin >> b[i];


}
